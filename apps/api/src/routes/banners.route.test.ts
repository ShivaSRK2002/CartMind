import { describe, it } from "vitest";

describe("GET /api/v1/banners", () => {
  it.todo("returns only active banners ordered by display_order");
  it.todo("returns an empty items array when no banners are active");
});
